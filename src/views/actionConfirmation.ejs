<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EASYCOOK</title>
    <link rel="stylesheet" href="/style/normalize.css">
    <link rel="stylesheet" href="/style/style.css">

</head>

<body>
    <div class="container">
        <%- include("./layouts/header") %>
        <%- include("./layouts/nav") %>
        <main>
            <div class="boxConfirmation">
                <% if (typeof(authorized) != 'undefined') { %>
                    <% if (authorized) { %>
                        <%- include("./layouts/navAdmin") %>
                    <% } %>
                <% } %>
                <h1 class="confirmation"> <%= confirmation.h1 %> </h1>
                <h2 class="confirmationDetails"><%- confirmation.h2 %></h2>
                <% if (typeof(tryAgain) != 'undefined') { %>
                    <h3><a href="/admin">Try Again</a></h3>
                <% } %>
            </div>
        </main>
        
        <%- include("./layouts/footer") %>
    </div>
</body>

</html>