<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EASYCOOK</title>
    <link rel="stylesheet" href="/style/normalize.css">
    <link rel="stylesheet" href="/style/style.css">

</head>

<body>
    <div class="container">
        <%- include("./layouts/header") %>
        <%- include("./layouts/nav") %>
        <%- include("./layouts/navAdmin") %>
        <main>
            
            <div class="listPostBox">
                <h1>Edit Post</h1>
                <% for (i in listPosts) {%>
                    <div class="listItem">
                        <% const postDate = `${listPosts[i].postDate.getDate()}/${listPosts[i].postDate.getMonth()+1}/${listPosts[i].postDate.getFullYear()}`;
                        const postHour = `${listPosts[i].postDate.getHours()}:${listPosts[i].postDate.getMinutes()}`%>
                        <p class="truncatedTitleText" ><%= listPosts[i].postTitle %></p>
                        <p><img src="../uploads/<%= listPosts[i].postImageName %>" alt="<%= listPosts[i].postTitle %> Photo"></p>
                        <p><a href="editPost/<%= listPosts[i].slug %>">Edit</a></p>
                        <p><a href="deletePost/<%= listPosts[i].slug %>" onclick="return confirmDelete('<%= listPosts[i].slug %>')">Delete</a></p>
                        <p><%= postDate %></p>
                    </div>
                <% } %>
                
            </div>


        </main>
        <%- include("./layouts/footer") %>
    </div>


    <script>
        function confirmDelete(slug) {
          return window.confirm("Confirm to delete this post?");
        }
    </script>
</body>

</html>